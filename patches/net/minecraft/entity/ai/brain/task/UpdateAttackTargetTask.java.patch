--- a/net/minecraft/entity/ai/brain/task/UpdateAttackTargetTask.java
+++ b/net/minecraft/entity/ai/brain/task/UpdateAttackTargetTask.java
@@ -36,13 +36,15 @@
    }
 
    protected void run(ServerWorld serverWorld, E mobEntity, long l) {
-      ((Optional)this.targetGetter.apply(mobEntity)).ifPresent((livingEntity) -> {
+      (this.targetGetter.apply(mobEntity)).ifPresent((livingEntity) -> {
          this.updateAttackTarget(mobEntity, livingEntity);
       });
    }
 
    private void updateAttackTarget(E entity, LivingEntity target) {
-      entity.getBrain().remember(MemoryModuleType.ATTACK_TARGET, (Object)target);
+      entity.getBrain().remember(MemoryModuleType.ATTACK_TARGET, target);
       entity.getBrain().forget(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE);
    }
 }
+
+// Fukkit Marker
